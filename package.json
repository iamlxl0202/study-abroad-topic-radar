{
  "name": "study-abroad-topic-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-report.mjs",
    "serve": "python3 -m http.server 4173",
    "check": "node --check app.js && node --check scripts/generate-report.mjs"
  }
}
